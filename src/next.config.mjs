/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'generate-ogp-images-with-ssg.vercel.app'
    ]
  }
};

export default nextConfig;
